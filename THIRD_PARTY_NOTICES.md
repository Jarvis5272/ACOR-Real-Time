# Third-Party Notices

ACOR links against zlib for deterministic gzip order-pack input. zlib is
distributed under the zlib License. The C++ standard library, POSIX threads,
and the Python standard library are supplied by the host toolchain/runtime.

No third-party reconstruction algorithm, early-stop model, training code, or
dataset is bundled in this source release.
